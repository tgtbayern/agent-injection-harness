# Rules kernel. Roles, night-action priority, vote legality and win conditions
# come from here rather than being hand-written; engine/ is a thin adapter.
werewolf-engine==0.1.0

# Dashboard backend. Not needed for the CLI, the harness or the test suite.
fastapi==0.115.0
uvicorn==0.30.6
pydantic>=2.9

# Dev only.
pytest>=7.4
httpx>=0.27          # fastapi TestClient

# Deliberately absent:
#   no agent framework (the ReAct loop is the thing being measured)
#   no openai sdk      (the gateway client speaks HTTP; the sdk is optional)
#   no numpy/scipy     (Wilson and bootstrap intervals are stdlib arithmetic)
#   no pyyaml          (configs are JSON)
